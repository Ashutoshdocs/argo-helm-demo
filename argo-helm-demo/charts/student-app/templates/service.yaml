apiVersion: v1

kind: Service

metadata:

  name: {{ include "student-app.fullname" . }}

spec:

  type: {{ .Values.service.type }}

  selector:

    app: student-app

  ports:

  - port: 80

    targetPort: 80
